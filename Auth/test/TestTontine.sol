pragma solidity 0.5.11;
/*
import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/Tontine.sol";
*/

contract TestTontine {
/*
	tontine_Cercle tontine = tontine_Cercle(DeployedAddresses.tontine_Cercle());

*/
 function testAddingNewTontine() public {
/*	
	string memory id_TontineExpected= "1"; 
string memory Nom_TontineExpected = "at";
 uint MontantExpected=10;
uint Nbr_participantsExpected = 2;
uint Nbr_cyclesExpected = 2;
string memory FrequenceExpected = "Par mois";
 uint OrdreExpected = 1;

tontine.addTontine(id_TontineExpected, Nom_TontineExpected, MontantExpected,Nbr_participantsExpected,Nbr_cyclesExpected,FrequenceExpected,OrdreExpected);
string memory id_Tontine;
string memory Nom_Tontine ;
 uint Montant;
uint Nbr_participants  ;
uint Nbr_cycles ;
string memory Frequence ;
 address createur ;
bool Etat;

	(  Nom_Tontine ,
  Montant,
 Nbr_participants  ,
 Nbr_cycles ,
  Frequence ,
  createur,
Etat) = tontine.getTontine("1");
*/       
/*Assert.equal(Nom_Tontine,Nom_TontineExpected, "Tontine name should match");
        Assert.equal(price, expectedPrice, "Item price should match");
      Assert.equal(status, uint(HashMarket.ItemStatus.active), "Item status at creation should be .active");
    Assert.equal(seller, this, "The function caller should be the seller");
*/    }

}
